# Portal build deps. jekyll-remote-theme pulls the shared theme by its pinned
# tag; github-pages-compatible Jekyll otherwise (build_type: workflow — we run
# Jekyll ourselves in Actions, no github-pages gem needed).
source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "jekyll-remote-theme", "~> 0.4"
